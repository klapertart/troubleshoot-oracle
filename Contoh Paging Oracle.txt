SELECT * 
FROM (
	SELECT TBL.*, ROWNUM AS RN
  FROM(
			SELECT NOP, TGL_PEREKAMAN, NIP_PEREKAM, TGL_PENDATAAN, NIP_PENDATA, TGL_PENELITI, NIP_PENELITIAN, NO_FORMULIR
      FROM NOP_KOREKSI
	) TBL
) WHERE ROWNUM <= ItemPerPage AND RN > ((Page-1)*ItemPerPage);